<li>
    <a href="{{ url }}" class="group flex gap-x-3 rounded-md p-2 text-sm font-semibold leading-6 {{ is_active ? 'bg-gray-800 text-white' : 'text-gray-400 hover:bg-gray-800 hover:text-white' }}">
        {{ content|raw }}
    </a>
</li>